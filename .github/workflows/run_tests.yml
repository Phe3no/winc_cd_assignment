name: remote update

on: push
jobs:
  test:
    name: Test
    # Specify on which operating system we want this workflow to run
    runs-on: ubuntu-20.04
    # The steps that will be excuted
    steps:
      - name: Configure SSH
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_SECRET_KEY }}
          known_hosts: "just-a-placeholder"

      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secreys.SSH_HOST }}{% endraw %} >> ~/.ssh/known_hosts

      - name: Execute ls -la
        run: ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} ls -la
